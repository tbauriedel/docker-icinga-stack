# docker-icinga-stack

Docker compose file to deploy a very lightweigt Icinga 2 setup for your test purposes.

Icinga Web 2 will be exposed to `http://localhost:8080`.

* Icinga Web 2 Login: `icinga` - `icinga`
* Icinga 2 API Credentials: `icinga` - `icinga`